function showEmail() {
  let div = document.querySelector("div.page__content")
  if (div.firstChild) {
    div.firstChild.remove()
  }
  div.insertAdjacentHTML("afterbegin", "<%= j render partial: 'email', locals: {email: @email, remote: true} %>")
}

showEmail()